# Голубой (среднесрочный) луп — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Реализовать дневную петлю-обёртку вокруг готового зелёного лупа: экран Властелина (корзина, воровство, подсчёт), награда/наказание, цепочка дней, концовки. Боссфайт — стаб.

**Architecture:** Стейт-машина дня (`enum DayPhase`) внутри `GameFlow` (подход A). Чистая логика (золото, прогрессия дней, воровство, пул наград) — отдельные классы/методы, покрытые EditMode-тестами. UI — два новых попапа + расширение `EndOfDayPopup`. Лут и `BankedGold` переживают смену дня без перезагрузки сцены.

**Tech Stack:** Unity 6000.3.16f1, URP 2D, uGUI (`UnityEngine.UI.Text/Button/Image`), NUnit EditMode тесты в `Code.Tests`, прогон через unityMCP `run_tests`. Спек: `docs/superpowers/specs/2026-05-29-blue-loop-design.md`.

**Соглашения по верификации:**
- **Тесты (чистая логика):** unityMCP `run_tests` с `mode="EditMode"`, `testFilter="Mimic.Tests.<ClassName>"`. Перед прогоном — `read_console` (фильтр Error) убедиться, что компиляция чистая.
- **Компиляция:** после правок скриптов — `refresh_unity`, затем `read_console` (Error). Ноль ошибок = OK.
- **Play-mode (UI/флоу):** `manage_editor` enter Play → действия → `read_console` + наблюдение. Описаны явные ожидаемые состояния.
- **Коммиты:** после каждой задачи. Ветка `main` — работаем прямо в ней (jam), но коммитим по задаче.

---

## File Structure

| Файл | Ответственность | Создать/Менять |
|------|-----------------|----------------|
| `Code/Game/GameResources.cs` | HP/ЖС/Held/Banked/Total, перенос между днями, снапшот дня | Менять |
| `Code/Data/LootData.cs` | + `Category`, `AcidRestoreOnDigest`, `DamageOnDigest`, `CanReturnToBasket`, `IsGlue`, `NeighborGoldPct`, `IsFixture` | Менять |
| `Code/Catalogs/LootCatalog.cs` | парс новых колонок с дефолтами; выборка по категории | Менять |
| `Code/Data/DayData.cs` | + `GoldDamageMult`, `RansomGold` | Менять |
| `Code/Catalogs/DayConfig.cs` | список всех дней, индекс текущего, `Advance()`, `IsLastDay` | Менять |
| `Code/Logic/TheftResolver.cs` | выбор предмета со свободной верхней гранью | Создать |
| `Code/Logic/Settlement.cs` | формула урона, выбор 3 предметов из пула | Создать |
| `Code/Logic/AdjacencyResolver.cs` | + исходящий эффект «−% золота соседям» | Менять |
| `Code/Game/GameFlow.cs` | стейт-машина `DayPhase`, оркестрация | Менять |
| `Code/Game/GameContext.cs` | банкование в корзину, доступ к Settlement, фикстуры | Менять |
| `Code/UI/OverlordPopup.cs` | корзина, воровство, кнопка итога, тултип | Создать |
| `Code/UI/RewardChoicePopup.cs` | выбор 1 из 3 | Создать |
| `Code/UI/EndOfDayPopup.cs` | тексты концовок, переиграть день, выкуп/вызов | Менять |
| `Resources/Configs/loot.csv.txt` | новые колонки + reward/punish/fixture предметы | Менять |
| `Resources/Configs/day.csv.txt` | + goldDamageMult, ransomGold | Менять |
| `Code.Tests/*` | новые тест-классы | Создать |

---

## Task 1: GameResources — Banked/Total золото и перенос между днями

**Files:**
- Modify: `Game/Assets/Code/Game/GameResources.cs`
- Test: `Game/Assets/Code.Tests/GameResourcesTests.cs` (создать)

- [ ] **Step 1: Написать падающий тест**

Создать `Game/Assets/Code.Tests/GameResourcesTests.cs`:

```csharp
using NUnit.Framework;
using Mimic.Game;
using Mimic.Data;

namespace Mimic.Tests
{
    public class GameResourcesTests
    {
        private static DayData Day(int day, int quota, int hp, int acid) =>
            new DayData { Day = day, GoldQuota = quota, StartHp = hp, StartAcid = acid,
                          AdventurerIds = new[] { "warrior" }, GoldDamageMult = 1f, RansomGold = 1000 };

        [Test]
        public void StartDay_FirstDay_InitsHpAcidAndQuota()
        {
            var r = new GameResources();
            r.StartDay(Day(1, 40, 3, 15), firstDay: true);
            Assert.AreEqual(3, r.CurrentHp);
            Assert.AreEqual(15, r.CurrentAcid);
            Assert.AreEqual(40, r.DayQuota);
            Assert.AreEqual(0, r.BankedGold);
        }

        [Test]
        public void StartDay_NextDay_KeepsHpAndBanked_RefillsAcid()
        {
            var r = new GameResources();
            r.StartDay(Day(1, 40, 5, 15), firstDay: true);
            r.CurrentHp = 2;          // получил урон
            r.CurrentAcid = 0;        // потратил ЖС
            r.BankedGold = 30;        // сдал в корзину
            r.StartDay(Day(2, 60, 5, 15), firstDay: false);
            Assert.AreEqual(2, r.CurrentHp, "HP переносится");
            Assert.AreEqual(15, r.CurrentAcid, "ЖС восстановлен");
            Assert.AreEqual(30, r.BankedGold, "Banked копится");
            Assert.AreEqual(60, r.DayQuota);
        }

        [Test]
        public void TotalGold_IsHeldPlusBanked()
        {
            var r = new GameResources();
            r.CurrentGoldInMimic = 25;
            r.BankedGold = 40;
            Assert.AreEqual(65, r.TotalGold);
        }

        [Test]
        public void Snapshot_RestoresHpAndBanked()
        {
            var r = new GameResources();
            r.CurrentHp = 5; r.BankedGold = 10;
            var snap = r.SnapshotDayStart();
            r.CurrentHp = 1; r.BankedGold = 50;
            r.RestoreDayStart(snap);
            Assert.AreEqual(5, r.CurrentHp);
            Assert.AreEqual(10, r.BankedGold);
        }
    }
}
```

- [ ] **Step 2: Прогнать тест — убедиться, что падает (не компилируется)**

unityMCP `run_tests` mode=`EditMode`, testFilter=`Mimic.Tests.GameResourcesTests`.
Ожидание: FAIL/ошибка компиляции — нет `firstDay`, `BankedGold`, `TotalGold`, `SnapshotDayStart`, `RestoreDayStart`, нет `GoldDamageMult`/`RansomGold` в `DayData`.

> Примечание: `DayData.GoldDamageMult`/`RansomGold` добавляются в Task 3, но тест Task 1 их использует в фабрике. Чтобы Task 1 компилировался автономно, добавить эти два поля в `DayData` прямо здесь (Step 3). Task 3 их парсит.

- [ ] **Step 3: Минимальная реализация**

Заменить `Game/Assets/Code/Game/GameResources.cs` целиком:

```csharp
namespace Mimic.Game
{
    public struct DayStartSnapshot
    {
        public int Hp;
        public int BankedGold;
    }

    public class GameResources
    {
        public int CurrentHp;
        public int CurrentAcid;
        public int CurrentGoldInMimic; // Held — живая сумма золота лута в мимике
        public int BankedGold;         // сдано в корзину, копится навсегда
        public int DayQuota;

        public int TotalGold => CurrentGoldInMimic + BankedGold;

        public void StartDay(Mimic.Data.DayData day, bool firstDay)
        {
            if (firstDay)
            {
                CurrentHp = day.StartHp;
                BankedGold = 0;
            }
            // HP и BankedGold переносятся со второго дня
            CurrentAcid = day.StartAcid; // ЖС всегда в фул
            DayQuota = day.GoldQuota;
            if (firstDay) CurrentGoldInMimic = 0;
        }

        public DayStartSnapshot SnapshotDayStart() =>
            new DayStartSnapshot { Hp = CurrentHp, BankedGold = BankedGold };

        public void RestoreDayStart(DayStartSnapshot s)
        {
            CurrentHp = s.Hp;
            BankedGold = s.BankedGold;
        }
    }
}
```

Добавить два поля в `Game/Assets/Code/Data/DayData.cs` (внутри класса):

```csharp
        public float GoldDamageMult;
        public int RansomGold;
```

- [ ] **Step 4: Прогнать тест — PASS**

unityMCP `run_tests` mode=`EditMode`, testFilter=`Mimic.Tests.GameResourcesTests`. Ожидание: 4 PASS.

> ⚠️ Этот шаг ломает существующий вызов `Resources.StartDay(DayConfig.Current)` в `GameContext.Awake` (теперь нужен 2-й аргумент). Чинится в Task 7. До тех пор проект может не компилироваться в Play — это ок, тесты EditMode компилируют только `Code` + `Code.Tests`. Если `read_console` покажет ошибку именно в `GameContext.cs:34`, временно поправить на `Resources.StartDay(DayConfig.Current, true)`.

- [ ] **Step 5: Закоммитить**

```bash
git add Game/Assets/Code/Game/GameResources.cs Game/Assets/Code/Data/DayData.cs Game/Assets/Code.Tests/GameResourcesTests.cs
git commit -m "feat(resources): banked/total gold + day-to-day carry + snapshot"
```

---

## Task 2: LootData/LootCatalog — новые поля и выборка по категории

**Files:**
- Modify: `Game/Assets/Code/Data/LootData.cs`
- Modify: `Game/Assets/Code/Catalogs/LootCatalog.cs`
- Test: `Game/Assets/Code.Tests/LootCatalogCategoryTests.cs` (создать)

- [ ] **Step 1: Написать падающий тест**

Создать `Game/Assets/Code.Tests/LootCatalogCategoryTests.cs`:

```csharp
using NUnit.Framework;
using Mimic.Data;

namespace Mimic.Tests
{
    public class LootDataDefaultsTests
    {
        [Test]
        public void NewFields_HaveSaneDefaults()
        {
            var d = new LootData();
            Assert.AreEqual(LootCategory.Normal, d.Category);
            Assert.AreEqual(0, d.AcidRestoreOnDigest);
            Assert.AreEqual(0, d.DamageOnDigest);
            Assert.IsTrue(d.CanReturnToBasket, "по умолчанию можно вернуть");
            Assert.IsFalse(d.IsGlue);
            Assert.IsFalse(d.IsFixture);
            Assert.AreEqual(0, d.NeighborGoldPct);
        }
    }
}
```

- [ ] **Step 2: Прогнать — FAIL (нет `LootCategory`, новых полей)**

unityMCP `run_tests` EditMode, filter `Mimic.Tests.LootDataDefaultsTests`. Ожидание: ошибка компиляции.

- [ ] **Step 3: Реализация**

Заменить `Game/Assets/Code/Data/LootData.cs` целиком:

```csharp
namespace Mimic.Data
{
    public enum LootCategory { Normal, Reward, Punish, Fixture }

    public class LootData
    {
        public string Id;
        public string Name;
        public string Description;
        public Shape Shape;
        public int Gold;
        public int AcidCost;
        public int HealOnDigest;
        public int CellsRestoredOnDigest;
        public string AdjacencyTarget;            // empty = no property
        public AdjacencyEffect[] AdjacencyEffects; // never null; empty if no property

        // --- голубой луп ---
        public LootCategory Category = LootCategory.Normal;
        public int AcidRestoreOnDigest;   // кислота/мизим: +ЖС при переваривании
        public int DamageOnDigest;        // гиря/клей/какашка: урон мимику при переваривании
        public bool CanReturnToBasket = true; // наказания = false
        public bool IsGlue;               // масса клея
        public bool IsFixture;            // сердце/желудок: нельзя двигать/переваривать
        public int NeighborGoldPct;       // какашка: -50 => соседям -50% золота
    }
}
```

Заменить тело `LootCatalog.Load()` в `Game/Assets/Code/Catalogs/LootCatalog.cs` (метод `Load`), сохранив `using`-и и сигнатуры:

```csharp
        public static void Load()
        {
            var ta = Resources.Load<TextAsset>("Configs/loot.csv");
            _byId = new Dictionary<string, LootData>();
            foreach (var row in CsvLoader.ParseAll(ta.text))
            {
                var d = new LootData
                {
                    Id = row[0],
                    Name = row[1],
                    Description = row[2],
                    Shape = Shape.Parse(row[3]),
                    Gold = int.Parse(row[4]),
                    AcidCost = int.Parse(row[5]),
                    HealOnDigest = int.Parse(row[6]),
                    CellsRestoredOnDigest = int.Parse(row[7]),
                    AdjacencyTarget = row[8],
                    AdjacencyEffects = AdjacencyEffect.ParseList(row[9]),
                    Category = ParseCategory(Col(row, 10, "normal")),
                    AcidRestoreOnDigest = int.Parse(Col(row, 11, "0")),
                    DamageOnDigest = int.Parse(Col(row, 12, "0")),
                    CanReturnToBasket = Col(row, 13, "1") != "0",
                    IsGlue = Col(row, 14, "0") == "1",
                    NeighborGoldPct = int.Parse(Col(row, 15, "0")),
                };
                d.IsFixture = d.Category == LootCategory.Fixture;
                _byId[d.Id] = d;
            }
        }

        private static string Col(System.Collections.Generic.IReadOnlyList<string> row, int i, string def)
            => i < row.Count && !string.IsNullOrEmpty(row[i]) ? row[i] : def;

        private static LootCategory ParseCategory(string s) => s switch
        {
            "reward"  => LootCategory.Reward,
            "punish"  => LootCategory.Punish,
            "fixture" => LootCategory.Fixture,
            _          => LootCategory.Normal,
        };

        public static System.Collections.Generic.List<LootData> ByCategory(LootCategory cat)
        {
            var list = new System.Collections.Generic.List<LootData>();
            foreach (var d in _byId.Values) if (d.Category == cat) list.Add(d);
            return list;
        }
```

> Проверить, что `CsvLoader.ParseAll` возвращает `List<IReadOnlyList<string>>` или `List<string[]>`. Если `string[]`, заменить тип параметра `Col` на `string[]` и `row.Count` на `row.Length`. (Открыть `Code/Logic/CsvLoader.cs` и сверить.)

- [ ] **Step 4: Прогнать — PASS**

unityMCP `run_tests` EditMode, filter `Mimic.Tests.LootDataDefaultsTests`. Ожидание: PASS. Также прогнать `Mimic.Tests.AdjacencyResolverTests` — должны остаться зелёными (старые колонки не сдвинуты).

- [ ] **Step 5: Закоммитить**

```bash
git add Game/Assets/Code/Data/LootData.cs Game/Assets/Code/Catalogs/LootCatalog.cs Game/Assets/Code.Tests/LootCatalogCategoryTests.cs
git commit -m "feat(loot): category + digest/basket/glue/neighbor fields"
```

---

## Task 3: DayConfig — список дней, прогрессия, новые поля

**Files:**
- Modify: `Game/Assets/Code/Catalogs/DayConfig.cs`
- Test: `Game/Assets/Code.Tests/DayConfigTests.cs` (создать)

> `DayData.GoldDamageMult`/`RansomGold` уже добавлены в Task 1.

- [ ] **Step 1: Написать падающий тест**

Создать `Game/Assets/Code.Tests/DayConfigTests.cs`. `DayConfig.Load()` читает из `Resources`, поэтому тестируем чистую прогрессию через тестовый сеттер списка:

```csharp
using NUnit.Framework;
using Mimic.Catalogs;
using Mimic.Data;

namespace Mimic.Tests
{
    public class DayConfigTests
    {
        private static DayData D(int n) => new DayData { Day = n, GoldQuota = n * 10,
            StartHp = 3, StartAcid = 15, AdventurerIds = new[] { "warrior" },
            GoldDamageMult = 2f, RansomGold = 500 };

        [Test]
        public void Advance_MovesToNextDay()
        {
            DayConfig.SetForTest(new[] { D(1), D(2), D(3) });
            Assert.AreEqual(1, DayConfig.Current.Day);
            Assert.IsFalse(DayConfig.IsLastDay);
            Assert.IsTrue(DayConfig.Advance());
            Assert.AreEqual(2, DayConfig.Current.Day);
        }

        [Test]
        public void Advance_OnLastDay_ReturnsFalse()
        {
            DayConfig.SetForTest(new[] { D(1), D(2) });
            DayConfig.Advance(); // -> day 2
            Assert.IsTrue(DayConfig.IsLastDay);
            Assert.IsFalse(DayConfig.Advance());
            Assert.AreEqual(2, DayConfig.Current.Day, "не уходит за последний день");
        }
    }
}
```

- [ ] **Step 2: Прогнать — FAIL (нет `SetForTest`, `Advance`, `IsLastDay`)**

unityMCP `run_tests` EditMode, filter `Mimic.Tests.DayConfigTests`.

- [ ] **Step 3: Реализация**

Заменить `Game/Assets/Code/Catalogs/DayConfig.cs` целиком:

```csharp
using System.Collections.Generic;
using Mimic.Logic;
using UnityEngine;

namespace Mimic.Catalogs
{
    public static class DayConfig
    {
        private static List<Mimic.Data.DayData> _days = new();
        private static int _index;

        public static Mimic.Data.DayData Current => _days[_index];
        public static bool IsLastDay => _index >= _days.Count - 1;

        public static void Load()
        {
            var ta = Resources.Load<TextAsset>("Configs/day.csv");
            var rows = CsvLoader.ParseAll(ta.text);
            if (rows.Count == 0)
                throw new System.InvalidOperationException("day.csv has no data rows");
            _days = new List<Mimic.Data.DayData>();
            foreach (var r in rows)
            {
                _days.Add(new Mimic.Data.DayData
                {
                    Day = int.Parse(r[0]),
                    GoldQuota = int.Parse(r[1]),
                    StartHp = int.Parse(r[2]),
                    StartAcid = int.Parse(r[3]),
                    AdventurerIds = r[4].Split(';'),
                    GoldDamageMult = r.Count > 5 && !string.IsNullOrEmpty(r[5]) ? float.Parse(r[5], System.Globalization.CultureInfo.InvariantCulture) : 1f,
                    RansomGold = r.Count > 6 && !string.IsNullOrEmpty(r[6]) ? int.Parse(r[6]) : 999999,
                });
            }
            _index = 0;
        }

        // true если перешли на следующий день; false если уже последний
        public static bool Advance()
        {
            if (IsLastDay) return false;
            _index++;
            return true;
        }

        public static void SetForTest(IEnumerable<Mimic.Data.DayData> days)
        {
            _days = new List<Mimic.Data.DayData>(days);
            _index = 0;
        }
    }
}
```

> Если `CsvLoader.ParseAll` возвращает `string[]` строки — заменить `r.Count` на `r.Length`.

- [ ] **Step 4: Прогнать — PASS**

unityMCP `run_tests` EditMode, filter `Mimic.Tests.DayConfigTests`. Также `Mimic.Tests.CsvLoaderTests` — зелёные.

- [ ] **Step 5: Закоммитить**

```bash
git add Game/Assets/Code/Catalogs/DayConfig.cs Game/Assets/Code.Tests/DayConfigTests.cs
git commit -m "feat(day): multi-day progression + damage mult + ransom threshold"
```

---

## Task 4: TheftResolver — выбор предмета со свободной верхней гранью

**Files:**
- Create: `Game/Assets/Code/Logic/TheftResolver.cs`
- Test: `Game/Assets/Code.Tests/TheftResolverTests.cs` (создать)

Определение из ТЗ: у предмета есть **свободная верхняя грань**, если у одной из его клеток все клетки по вертикали вверх до верхней границы сетки пусты. В `GridModel` верх = больший `y` (см. `GridModel.TryPlace`: `gy = y + (sh-1-r)`, row 0 = визуальный верх = больший y). Значит для клетки `(cx, cy)` «вверх» = `cy+1 .. Height-1`.

- [ ] **Step 1: Написать падающий тест**

Создать `Game/Assets/Code.Tests/TheftResolverTests.cs`:

```csharp
using NUnit.Framework;
using Mimic.Logic;
using Mimic.Data;

namespace Mimic.Tests
{
    public class TheftResolverTests
    {
        private class Item { public string Id; public Shape Shape; }
        private static Item Mk(string id, string shape) => new Item { Id = id, Shape = Shape.Parse(shape) };

        [Test]
        public void TopItem_HasFreeTopEdge()
        {
            var grid = new GridModel<Item>(4, 4);
            var a = Mk("a", "X");
            grid.TryPlace(a, 0, 3, Rotation.Deg0); // y=3 = верхняя граница
            Assert.IsTrue(TheftResolver.HasFreeTopEdge(grid, a, i => i));
        }

        [Test]
        public void CoveredItem_HasNoFreeTopEdge()
        {
            var grid = new GridModel<Item>(4, 4);
            var bottom = Mk("bottom", "X");
            var top = Mk("top", "X");
            grid.TryPlace(bottom, 0, 0, Rotation.Deg0);
            grid.TryPlace(top, 0, 1, Rotation.Deg0); // прямо над bottom
            Assert.IsFalse(TheftResolver.HasFreeTopEdge(grid, bottom, i => i),
                "над bottom стоит top -> грань закрыта");
            Assert.IsTrue(TheftResolver.HasFreeTopEdge(grid, top, i => i));
        }

        [Test]
        public void PickStealable_ReturnsNull_WhenNoneFree()
        {
            var grid = new GridModel<Item>(2, 2);
            var b = Mk("b", "X");
            var t = Mk("t", "X");
            grid.TryPlace(b, 0, 0, Rotation.Deg0);
            grid.TryPlace(t, 0, 1, Rotation.Deg0);
            // b закрыт t; t свободен -> вернёт t
            var picked = TheftResolver.PickStealable(grid, i => i, seed: 0);
            Assert.AreSame(t, picked);
        }

        [Test]
        public void PickStealable_Deterministic_WithSeed()
        {
            var grid = new GridModel<Item>(4, 1);
            var x = Mk("x", "X");
            var y = Mk("y", "X");
            grid.TryPlace(x, 0, 0, Rotation.Deg0);
            grid.TryPlace(y, 2, 0, Rotation.Deg0);
            var p1 = TheftResolver.PickStealable(grid, i => i, seed: 42);
            var p2 = TheftResolver.PickStealable(grid, i => i, seed: 42);
            Assert.AreSame(p1, p2);
            Assert.IsNotNull(p1);
        }
    }
}
```

- [ ] **Step 2: Прогнать — FAIL (нет `TheftResolver`)**

unityMCP `run_tests` EditMode, filter `Mimic.Tests.TheftResolverTests`.

- [ ] **Step 3: Реализация**

Создать `Game/Assets/Code/Logic/TheftResolver.cs`:

```csharp
using System;
using System.Collections.Generic;

namespace Mimic.Logic
{
    public static class TheftResolver
    {
        // У предмета свободная верхняя грань, если есть его клетка, над которой
        // (по тому же x, y+1..Height-1) все клетки пусты.
        public static bool HasFreeTopEdge<T>(GridModel<T> grid, T item, Func<T, T> id) where T : class
        {
            for (int x = 0; x < grid.Width; x++)
            {
                for (int y = 0; y < grid.Height; y++)
                {
                    if (!ReferenceEquals(grid.GetAt(x, y), item)) continue;
                    bool clearAbove = true;
                    for (int yy = y + 1; yy < grid.Height; yy++)
                        if (grid.GetAt(x, yy) != null) { clearAbove = false; break; }
                    if (clearAbove) return true;
                }
            }
            return false;
        }

        // Случайный предмет со свободной верхней гранью, либо null.
        // id — функция приведения (в тестах i=>i; в проде i=>i для T=LootView).
        public static T PickStealable<T>(GridModel<T> grid, Func<T, T> id, int seed) where T : class
        {
            var candidates = new List<T>();
            foreach (var item in grid.AllItems())
                if (HasFreeTopEdge(grid, item, id)) candidates.Add(item);
            if (candidates.Count == 0) return null;
            var rng = new Random(seed);
            return candidates[rng.Next(candidates.Count)];
        }
    }
}
```

> `AllItems()` возвращает `placements.Keys` — порядок недетерминирован между запусками, но в рамках одного запуска стабилен; тест `Deterministic_WithSeed` дважды вызывает на одном гриде, поэтому набор кандидатов идентичен и seed даёт один индекс. OK.

- [ ] **Step 4: Прогнать — PASS**

unityMCP `run_tests` EditMode, filter `Mimic.Tests.TheftResolverTests`. Ожидание: 4 PASS.

- [ ] **Step 5: Закоммитить**

```bash
git add Game/Assets/Code/Logic/TheftResolver.cs Game/Assets/Code.Tests/TheftResolverTests.cs
git commit -m "feat(logic): theft resolver — free-top-edge pick"
```

---

## Task 5: Settlement — формула урона и выбор 3 предметов из пула

**Files:**
- Create: `Game/Assets/Code/Logic/Settlement.cs`
- Test: `Game/Assets/Code.Tests/SettlementTests.cs` (создать)

- [ ] **Step 1: Написать падающий тест**

Создать `Game/Assets/Code.Tests/SettlementTests.cs`:

```csharp
using NUnit.Framework;
using Mimic.Logic;
using System.Collections.Generic;

namespace Mimic.Tests
{
    public class SettlementTests
    {
        [Test]
        public void Damage_Zero_WhenTotalMeetsQuota()
        {
            Assert.AreEqual(0, Settlement.Damage(total: 40, quota: 40, mult: 2f));
            Assert.AreEqual(0, Settlement.Damage(total: 55, quota: 40, mult: 2f));
        }

        [Test]
        public void Damage_ShortfallTimesMult_RoundedUp()
        {
            // недобор 10, mult 2 => 20
            Assert.AreEqual(20, Settlement.Damage(total: 30, quota: 40, mult: 2f));
            // недобор 5, mult 0.5 => 2.5 -> ceil 3
            Assert.AreEqual(3, Settlement.Damage(total: 35, quota: 40, mult: 0.5f));
        }

        [Test]
        public void Pick3_ReturnsUpToThree_NoDuplicates()
        {
            var pool = new List<string> { "a", "b", "c", "d", "e" };
            var picked = Settlement.Pick3(pool, seed: 1);
            Assert.AreEqual(3, picked.Count);
            CollectionAssert.AllItemsAreUnique(picked);
            foreach (var p in picked) Assert.Contains(p, pool);
        }

        [Test]
        public void Pick3_SmallPool_ReturnsAll()
        {
            var pool = new List<string> { "a", "b" };
            var picked = Settlement.Pick3(pool, seed: 1);
            Assert.AreEqual(2, picked.Count);
        }
    }
}
```

- [ ] **Step 2: Прогнать — FAIL (нет `Settlement`)**

unityMCP `run_tests` EditMode, filter `Mimic.Tests.SettlementTests`.

- [ ] **Step 3: Реализация**

Создать `Game/Assets/Code/Logic/Settlement.cs`:

```csharp
using System;
using System.Collections.Generic;

namespace Mimic.Logic
{
    public static class Settlement
    {
        // Урон = недостающее золото * множитель, округление вверх. 0 если квота закрыта.
        public static int Damage(int total, int quota, float mult)
        {
            int shortfall = quota - total;
            if (shortfall <= 0) return 0;
            return (int)Math.Ceiling(shortfall * mult);
        }

        // До 3 различных элементов из пула (Fisher–Yates по копии).
        public static List<T> Pick3<T>(IReadOnlyList<T> pool, int seed)
        {
            var copy = new List<T>(pool);
            var rng = new Random(seed);
            for (int i = copy.Count - 1; i > 0; i--)
            {
                int j = rng.Next(i + 1);
                (copy[i], copy[j]) = (copy[j], copy[i]);
            }
            int take = Math.Min(3, copy.Count);
            return copy.GetRange(0, take);
        }
    }
}
```

- [ ] **Step 4: Прогнать — PASS**

unityMCP `run_tests` EditMode, filter `Mimic.Tests.SettlementTests`. Ожидание: 4 PASS.

- [ ] **Step 5: Закоммитить**

```bash
git add Game/Assets/Code/Logic/Settlement.cs Game/Assets/Code.Tests/SettlementTests.cs
git commit -m "feat(logic): settlement damage formula + reward pool pick"
```

---

## Task 6: AdjacencyResolver — исходящий эффект «−% золота соседям» (какашка)

**Files:**
- Modify: `Game/Assets/Code/Logic/AdjacencyResolver.cs`
- Modify: `Game/Assets/Code/Game/GameContext.cs` (передать `NeighborGoldPct` в Resolve)
- Test: `Game/Assets/Code.Tests/NeighborGoldTests.cs` (создать)

Текущий `Resolve` применяет эффект к **себе**, когда рядом target. Какашке нужен **исходящий** эффект: она снижает золото всех 4-соседей на `NeighborGoldPct`%. Добавляем опциональный делегат `neighborGoldPctOf`.

- [ ] **Step 1: Прочитать остаток `AdjacencyResolver.cs`**

Открыть `Game/Assets/Code/Logic/AdjacencyResolver.cs` целиком (Read), чтобы видеть, как считаются 4-соседи и формируется `TotalGold` — расширение должно переиспользовать тот же обход соседей и пересчитать `TotalGold` в конце.

- [ ] **Step 2: Написать падающий тест**

Создать `Game/Assets/Code.Tests/NeighborGoldTests.cs`:

```csharp
using NUnit.Framework;
using Mimic.Logic;
using Mimic.Data;

namespace Mimic.Tests
{
    public class NeighborGoldTests
    {
        private class Item
        {
            public string Id; public Shape Shape; public int Gold; public int AcidCost;
            public string AdjacencyTarget; public AdjacencyEffect[] AdjacencyEffects;
            public int NeighborGoldPct;
        }
        private static Item Mk(string id, int gold, int npct = 0) => new Item {
            Id = id, Shape = Shape.Parse("X"), Gold = gold, AcidCost = 1,
            AdjacencyTarget = null, AdjacencyEffects = AdjacencyEffect.ParseList(null),
            NeighborGoldPct = npct };

        [Test]
        public void Poop_Reduces_NeighborGold_By50pct()
        {
            var grid = new GridModel<Item>(4, 4);
            var gem = Mk("gem", 20);
            var poop = Mk("poop", 0, npct: -50);
            grid.TryPlace(gem, 0, 0, Rotation.Deg0);
            grid.TryPlace(poop, 1, 0, Rotation.Deg0); // сосед gem по x
            var res = AdjacencyResolver.Resolve(grid, i => i.Id, i => i.Gold, i => i.AcidCost,
                i => i.AdjacencyTarget, i => i.AdjacencyEffects, i => i.NeighborGoldPct);
            Assert.AreEqual(10, res.GetGold(gem), "20 * (1 - 0.5)");
        }

        [Test]
        public void NoPoop_KeepsGold()
        {
            var grid = new GridModel<Item>(4, 4);
            var gem = Mk("gem", 20);
            grid.TryPlace(gem, 0, 0, Rotation.Deg0);
            var res = AdjacencyResolver.Resolve(grid, i => i.Id, i => i.Gold, i => i.AcidCost,
                i => i.AdjacencyTarget, i => i.AdjacencyEffects, i => i.NeighborGoldPct);
            Assert.AreEqual(20, res.GetGold(gem));
        }
    }
}
```

- [ ] **Step 3: Реализация — добавить перегрузку `Resolve` с `neighborGoldPctOf`**

В `AdjacencyResolver` сохранить существующий 6-аргументный `Resolve` (вызвать новый с `_ => 0`), и добавить 7-аргументную перегрузку. После существующего шага применения adjacency-эффектов и **до** финального суммирования `TotalGold` вставить блок:

```csharp
            // Исходящий эффект: предмет с NeighborGoldPic != 0 снижает золото 4-соседей.
            // Переиспользуем тот же обход соседей, что и выше (4-соседство по клеткам).
            foreach (var src in grid.AllItems())
            {
                int pct = neighborGoldPctOf(src);
                if (pct == 0) continue;
                foreach (var nb in Neighbors4(grid, src))
                {
                    if (ReferenceEquals(nb, src)) continue;
                    int g = result.EffectiveGold[nb];
                    result.EffectiveGold[nb] = (int)System.Math.Round(g * (1f + pct / 100f));
                }
            }

            // Пересчитать TotalGold после исходящих эффектов
            result.TotalGold = 0;
            foreach (var item in grid.AllItems())
                result.TotalGold += result.EffectiveGold[item];
```

Где `Neighbors4` — приватный хелпер, возвращающий уникальных 4-соседей предмета по его клеткам:

```csharp
        private static IEnumerable<T> Neighbors4<T>(GridModel<T> grid, T item) where T : class
        {
            var seen = new HashSet<T>();
            int[] dx = { 1, -1, 0, 0 };
            int[] dy = { 0, 0, 1, -1 };
            for (int x = 0; x < grid.Width; x++)
                for (int y = 0; y < grid.Height; y++)
                {
                    if (!ReferenceEquals(grid.GetAt(x, y), item)) continue;
                    for (int k = 0; k < 4; k++)
                    {
                        var nb = grid.GetAt(x + dx[k], y + dy[k]);
                        if (nb != null && !ReferenceEquals(nb, item) && seen.Add(nb)) yield return nb;
                    }
                }
        }
```

> Если в существующем коде уже есть приватный обход соседей — переиспользовать его вместо дублирования (DRY). Сверить при чтении на Step 1.

Сигнатура новой перегрузки:

```csharp
        public static AdjacencyResult<T> Resolve<T>(
            GridModel<T> grid,
            Func<T, string> idOf, Func<T, int> baseGoldOf, Func<T, int> baseAcidOf,
            Func<T, string> adjacencyTargetOf, Func<T, AdjacencyEffect[]> adjacencyEffectsOf,
            Func<T, int> neighborGoldPctOf) where T : class
```

И старая 6-арг версия делегирует: `=> Resolve(grid, idOf, baseGoldOf, baseAcidOf, adjacencyTargetOf, adjacencyEffectsOf, _ => 0);`

- [ ] **Step 4: Обновить вызов в `GameContext.OnGridChanged`**

В `Game/Assets/Code/Game/GameContext.cs` метод `OnGridChanged`, добавить 7-й аргумент в вызов `AdjacencyResolver.Resolve`:

```csharp
                v => v.Data.AdjacencyEffects,
                v => v.Data.NeighborGoldPct);
```

- [ ] **Step 5: Прогнать — PASS**

unityMCP `run_tests` EditMode, filter `Mimic.Tests.NeighborGoldTests`; затем `Mimic.Tests.AdjacencyResolverTests` (регрессия — все зелёные, т.к. старая перегрузка сохранена).

- [ ] **Step 6: Закоммитить**

```bash
git add Game/Assets/Code/Logic/AdjacencyResolver.cs Game/Assets/Code/Game/GameContext.cs Game/Assets/Code.Tests/NeighborGoldTests.cs
git commit -m "feat(adjacency): outgoing neighbor-gold debuff (poop)"
```

---

## Task 7: GameFlow — стейт-машина `DayPhase` + вход в фазу Властелина

**Files:**
- Modify: `Game/Assets/Code/Game/GameFlow.cs`
- Modify: `Game/Assets/Code/Game/GameContext.cs:34` (StartDay firstDay)

Цель задачи: заменить прямой `EndDay→EndPopup` на переход в фазу `Overlord`, и подготовить каркас фаз. Сами попапы Властелина/награды подключаются в Task 8–9 (пока — заглушки-вызовы, которые ведут в Transition).

- [ ] **Step 1: Починить вызов StartDay в GameContext**

`Game/Assets/Code/Game/GameContext.cs:34`:

```csharp
            Resources.StartDay(DayConfig.Current, firstDay: true);
```

- [ ] **Step 2: Переписать `GameFlow.cs` с фазами**

Заменить `Game/Assets/Code/Game/GameFlow.cs` целиком:

```csharp
using System.Collections.Generic;
using UnityEngine;
using Mimic.Catalogs;
using Mimic.Data;
using Mimic.Logic;
using Mimic.UI;

namespace Mimic.Game
{
    public enum DayPhase { Adventurers, Overlord, RewardChoice, Transition }

    public class GameFlow : MonoBehaviour
    {
        [Header("Popups")]
        public AdventurerIntroPopup IntroPopup;
        public SurrenderConfirmPopup SurrenderPopup;
        public EndOfDayPopup EndPopup;
        public OverlordPopup OverlordPopup;
        public RewardChoicePopup RewardPopup;

        [Header("Scene refs")]
        public HudView Hud;

        public DayPhase Phase { get; private set; }

        private Queue<string> queue;
        private int totalInDay;
        private int processed;
        private AdventurerData current;
        private bool dayEnded;
        private DayStartSnapshot daySnapshot;

        private void Start()
        {
            BeginDay(firstDay: true);
            Hud.NextButton.onClick.AddListener(NextOrEndDay);
            Hud.SurrenderButton.onClick.AddListener(() => SurrenderPopup.Show(EndBurst));
        }

        private void BeginDay(bool firstDay)
        {
            var ctx = GameContext.Instance;
            if (!firstDay) ctx.Resources.StartDay(DayConfig.Current, firstDay: false);
            daySnapshot = ctx.Resources.SnapshotDayStart();

            Phase = DayPhase.Adventurers;
            dayEnded = false;
            queue = new Queue<string>(DayConfig.Current.AdventurerIds);
            totalInDay = DayConfig.Current.AdventurerIds.Length;
            processed = 0;
            Hud.SetDayCounter(DayConfig.Current.Day);
            Hud.SetNextButtonLabel("Следующий!");
            ctx.OnGridChanged();
            BringNext();
        }

        private void BringNext()
        {
            if (queue.Count == 0)
            {
                Hud.SetNextButtonLabel("Завершить день");
                Hud.SetNextButtonEnabled(true);
                return;
            }
            string id = queue.Dequeue();
            current = AdventurerCatalog.Get(id);
            processed++;
            Hud.SetHeroCounter(processed, totalInDay);
            IntroPopup.Show(current, OnEatPressed);
        }

        private void OnEatPressed()
        {
            var ctx = GameContext.Instance;
            foreach (var lootId in current.LootIds)
            {
                var data = LootCatalog.Get(lootId);
                var view = ctx.SpawnLoot(data, ctx.AdventurerGrid.CellsRoot);
                if (!TryPlaceFirstFit(ctx.AdventurerGrid, view))
                {
                    Destroy(view.gameObject);
                    continue;
                }
            }
            Hud.SetNextButtonEnabled(false);
            ctx.OnGridChanged();
        }

        public static bool TryPlaceFirstFit(GridView grid, LootView view)
        {
            for (int y = 0; y < grid.Height; y++)
                for (int x = 0; x < grid.Width; x++)
                    if (grid.Model.TryPlace(view, x, y, Rotation.Deg0))
                    {
                        var rt = (RectTransform)view.transform;
                        rt.SetParent(grid.CellsRoot, worldPositionStays: false);
                        rt.anchorMin = rt.anchorMax = new Vector2(0, 0);
                        rt.pivot = new Vector2(0, 0);
                        rt.position = grid.CellRects[x, y].position;
                        return true;
                    }
            return false;
        }

        public void NextOrEndDay()
        {
            if (Phase != DayPhase.Adventurers) return;
            var ctx = GameContext.Instance;
            if (ctx.AdventurerGrid.Model.FreeCellsCount < ctx.AdventurerGrid.Width * ctx.AdventurerGrid.Height)
                return; // нельзя дальше, пока грид приключенца не пуст

            if (queue.Count == 0) EnterOverlord();
            else BringNext();
        }

        // --- Фаза Властелина ---
        private void EnterOverlord()
        {
            Phase = DayPhase.Overlord;
            OverlordPopup.Show(OnSettled);
        }

        // Колбэк от OverlordPopup после "Подвести итог дня".
        // damageApplied уже нанесён попапом; здесь решаем смерть/награду.
        private void OnSettled()
        {
            var ctx = GameContext.Instance;
            if (ctx.Resources.CurrentHp <= 0)
            {
                EndDeath();
                return;
            }
            EnterReward();
        }

        private void EnterReward()
        {
            Phase = DayPhase.RewardChoice;
            var ctx = GameContext.Instance;
            bool win = ctx.Resources.TotalGold >= ctx.Resources.DayQuota;
            RewardPopup.Show(win, onTaken: OnRewardTaken, onOvereat: EndBurst);
        }

        private void OnRewardTaken()
        {
            EnterTransition();
        }

        // --- Переход / финалы ---
        private void EnterTransition()
        {
            Phase = DayPhase.Transition;
            var ctx = GameContext.Instance;
            bool canRansom = ctx.Resources.TotalGold >= DayConfig.Current.RansomGold;
            bool hasNextDay = !DayConfig.IsLastDay;
            EndPopup.ShowTransition(
                hasNextDay: hasNextDay,
                canRansom: canRansom,
                onNextDay: GoNextDay,
                onRansom: EndRansomWin,
                onChallenge: EndChallengeStub);
        }

        private void GoNextDay()
        {
            DayConfig.Advance();
            BeginDay(firstDay: false);
        }

        private void EndRansomWin() => EndPopup.ShowRansomWin();
        private void EndChallengeStub() => EndPopup.ShowChallengeStub();

        private void EndDeath()
        {
            if (dayEnded) return;
            dayEnded = true;
            EndPopup.ShowDeath(onRetryDay: RetryDay);
        }

        private void EndBurst()
        {
            if (dayEnded) return;
            dayEnded = true;
            EndPopup.ShowBurst(onRetryDay: RetryDay);
        }

        private void RetryDay()
        {
            var ctx = GameContext.Instance;
            ctx.Resources.RestoreDayStart(daySnapshot);
            // Содержимое сетки мимика на момент снапшота не воспроизводим в MVP
            // (jam): откат только HP/Banked, лут остаётся как был. Достаточно для
            // повторной попытки закрыть квоту. Перезагрузка дня:
            BeginDay(firstDay: false);
        }

        private void Update()
        {
            var ctx = GameContext.Instance;
            if (ctx == null || Hud == null || Phase != DayPhase.Adventurers) return;
            bool advEmpty = ctx.AdventurerGrid.Model.FreeCellsCount == ctx.AdventurerGrid.Width * ctx.AdventurerGrid.Height;
            Hud.SetNextButtonEnabled(advEmpty);
        }
    }
}
```

> Этот файл ссылается на `OverlordPopup` (Task 8), `RewardChoicePopup` (Task 9) и новые методы `EndOfDayPopup` (Task 10), которых ещё нет — проект не скомпилируется до завершения Task 10. Это ожидаемо: Task 7–10 — один связный блок. Коммитим Task 7 как «каркас», но **компиляцию проверяем в конце Task 10**.

- [ ] **Step 3: Закоммитить каркас**

```bash
git add Game/Assets/Code/Game/GameFlow.cs Game/Assets/Code/Game/GameContext.cs
git commit -m "feat(flow): DayPhase state machine skeleton (compiles after Task 10)"
```

---

## Task 8: OverlordPopup — корзина, воровство, подсчёт, тултип

**Files:**
- Create: `Game/Assets/Code/UI/OverlordPopup.cs`
- Modify: `Game/Assets/Code/Game/GameContext.cs` (метод `BankItem`)

`OverlordPopup` показывается поверх игрового HUD. В MVP правый грид (`AdventurerGrid`) служит зоной корзины: предметы, перетащенные туда игроком, считаем «сданными». Чтобы не переписывать DragController, реализуем корзину упрощённо: **кнопка «Сдать выделенный»/перетаскивание в правый грид → `GameContext.BankItem`**. Для jam берём простую модель: попап даёт кнопку «Подвести итог дня», а сдача предметов выполняется перетаскиванием в правый грид (уже работает drag между гридами) — при подсчёте всё, что лежит в правом гриде, банкуется.

- [ ] **Step 1: Добавить `BankItem`/`BankAllInGrid` в GameContext**

В `Game/Assets/Code/Game/GameContext.cs` добавить методы:

```csharp
        // Сдать предмет Властелину: золото (с adjacency) -> BankedGold, предмет удаляется.
        public void BankItem(LootView item)
        {
            if (item.Data.IsFixture) return;
            int gold = LastResolved != null ? LastResolved.GetGold(item) : item.Data.Gold;
            Resources.BankedGold += gold;
            MimicGrid.Model.Remove(item);
            Destroy(item.gameObject);
        }

        // Сдать всё, что игрок переложил в правый (корзинный) грид.
        public int BankAllInGrid(GridView basket)
        {
            int total = 0;
            var items = new System.Collections.Generic.List<LootView>(basket.Model.AllItems());
            foreach (var it in items)
            {
                if (it.Data.IsFixture) continue;
                total += it.Data.Gold; // в корзине adjacency не считаем — берём базовое золото
                Resources.BankedGold += it.Data.Gold;
                basket.Model.Remove(it);
                Destroy(it.gameObject);
            }
            return total;
        }
```

- [ ] **Step 2: Создать `OverlordPopup.cs`**

Создать `Game/Assets/Code/UI/OverlordPopup.cs`:

```csharp
using System;
using UnityEngine;
using UnityEngine.UI;
using Mimic.Game;
using Mimic.Catalogs;
using Mimic.Logic;

namespace Mimic.UI
{
    // Экран Властелина. Правый грид (AdventurerGrid) используется как корзина:
    // игрок перетаскивает туда лут, который хочет сдать. По "Подвести итог дня"
    // всё в корзине банкуется, происходит воровство и подсчёт.
    public class OverlordPopup : MonoBehaviour
    {
        public GameObject Root;          // панель попапа (заголовок + кнопка)
        public Text TitleText;
        public Text GoldText;            // [есть]/[надо]
        public Button SettleButton;
        public Text SettleLabel;
        public Text TooltipText;         // под кнопкой: HP-урон + награда/наказание

        private Action onSettled;
        private int theftSeed = 1;

        private void Awake()
        {
            if (Root != null) Root.SetActive(false);
            if (SettleButton != null) SettleButton.onClick.AddListener(OnSettleClicked);
        }

        public void Show(Action settledCallback)
        {
            onSettled = settledCallback;
            if (Root != null) Root.SetActive(true);
            if (TitleText != null) TitleText.text = "Властелин пришёл";
            if (SettleLabel != null) SettleLabel.text = "Подвести итог дня";
            RefreshTexts();
        }

        // Вызывать при изменениях грида во время фазы Властелина (по событию OnGridChanged).
        public void RefreshTexts()
        {
            var r = GameContext.Instance.Resources;
            if (GoldText != null) GoldText.text = $"{r.TotalGold}/{r.DayQuota}";
            if (TooltipText != null)
            {
                int dmg = Settlement.Damage(r.TotalGold, r.DayQuota, DayConfig.Current.GoldDamageMult);
                TooltipText.text = dmg > 0
                    ? $"Потеря HP: -{dmg}\nПоследует наказание"
                    : "Последует награда";
            }
        }

        private void OnSettleClicked()
        {
            var ctx = GameContext.Instance;

            // 1. Банкуем всё, что игрок переложил в корзину (правый грид)
            ctx.BankAllInGrid(ctx.AdventurerGrid);

            // 2. Воровство: 1 предмет со свободной верхней гранью из мимика
            var stolen = TheftResolver.PickStealable(ctx.MimicGrid.Model, v => v, theftSeed++);
            if (stolen != null)
            {
                ctx.MimicGrid.Model.Remove(stolen);
                Destroy(stolen.gameObject);
            }
            ctx.OnGridChanged();

            // 3. Подсчёт и урон
            var r = ctx.Resources;
            int dmg = Settlement.Damage(r.TotalGold, r.DayQuota, DayConfig.Current.GoldDamageMult);
            if (dmg > 0) r.CurrentHp -= dmg;

            if (Root != null) Root.SetActive(false);
            onSettled?.Invoke();
        }
    }
}
```

- [ ] **Step 3: Компиляцию проверяем после Task 10. Закоммитить.**

```bash
git add Game/Assets/Code/UI/OverlordPopup.cs Game/Assets/Code/Game/GameContext.cs
git commit -m "feat(ui): OverlordPopup — basket banking, theft, settlement"
```

---

## Task 9: RewardChoicePopup — выбор 1 из 3

**Files:**
- Create: `Game/Assets/Code/UI/RewardChoicePopup.cs`

- [ ] **Step 1: Создать `RewardChoicePopup.cs`**

Создать `Game/Assets/Code/UI/RewardChoicePopup.cs`:

```csharp
using System;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using Mimic.Game;
using Mimic.Catalogs;
using Mimic.Data;
using Mimic.Logic;

namespace Mimic.UI
{
    // Выбор 1 из 3 предметов-наград или наказаний. Взятый кладётся в мимик first-fit;
    // если не влезает -> onOvereat (поражение "лопнул").
    public class RewardChoicePopup : MonoBehaviour
    {
        public GameObject Root;
        public Text TitleText;
        public Button[] OptionButtons = new Button[3]; // 3 кнопки выбора
        public Text[] OptionLabels = new Text[3];

        private Action onTaken;
        private Action onOvereat;
        private List<LootData> options;
        private int pickSeed = 1;

        private void Awake()
        {
            if (Root != null) Root.SetActive(false);
            for (int i = 0; i < OptionButtons.Length; i++)
            {
                int idx = i;
                if (OptionButtons[i] != null)
                    OptionButtons[i].onClick.AddListener(() => Take(idx));
            }
        }

        public void Show(bool win, Action onTaken, Action onOvereat)
        {
            this.onTaken = onTaken;
            this.onOvereat = onOvereat;

            var pool = LootCatalog.ByCategory(win ? LootCategory.Reward : LootCategory.Punish);
            options = Settlement.Pick3(pool, pickSeed++);

            if (Root != null) Root.SetActive(true);
            if (TitleText != null) TitleText.text = win ? "Награда — заберите 1" : "Наказание — заберите 1";
            for (int i = 0; i < OptionButtons.Length; i++)
            {
                bool has = i < options.Count;
                if (OptionButtons[i] != null) OptionButtons[i].gameObject.SetActive(has);
                if (has && OptionLabels[i] != null) OptionLabels[i].text = options[i].Name;
            }
        }

        private void Take(int idx)
        {
            if (options == null || idx >= options.Count) return;
            var ctx = GameContext.Instance;
            var view = ctx.SpawnLoot(options[idx], ctx.MimicGrid.CellsRoot);
            bool placed = GameFlow.TryPlaceFirstFit(ctx.MimicGrid, view);
            if (Root != null) Root.SetActive(false);
            if (!placed)
            {
                Destroy(view.gameObject);
                onOvereat?.Invoke();   // не влез -> лопнул
                return;
            }
            ctx.OnGridChanged();
            onTaken?.Invoke();
        }
    }
}
```

- [ ] **Step 2: Компиляцию проверяем после Task 10. Закоммитить.**

```bash
git add Game/Assets/Code/UI/RewardChoicePopup.cs
git commit -m "feat(ui): RewardChoicePopup — pick 1 of 3, place or burst"
```

---

## Task 10: EndOfDayPopup — концовки, переход, переиграть день

**Files:**
- Modify: `Game/Assets/Code/UI/EndOfDayPopup.cs`

- [ ] **Step 1: Переписать `EndOfDayPopup.cs`**

Заменить `Game/Assets/Code/UI/EndOfDayPopup.cs` целиком. Поддерживает: переход (3 кнопки), выкуп-вин, стаб-вызов, смерть, лопнул. Тексты из ТЗ.

```csharp
using System;
using UnityEngine;
using UnityEngine.UI;
using UnityEngine.SceneManagement;

namespace Mimic.UI
{
    public class EndOfDayPopup : MonoBehaviour
    {
        public Text TitleText;
        public Text SubtitleText;
        public Button PrimaryButton;    // следующий день / переиграть / в меню
        public Text PrimaryLabel;
        public Button SecondaryButton;  // выкуп / в меню
        public Text SecondaryLabel;
        public Button TertiaryButton;   // вызов
        public Text TertiaryLabel;

        private Action primary, secondary, tertiary;

        private void Awake() => gameObject.SetActive(false);

        private void Bind(Button b, Text label, string text, Action cb, ref Action slot, bool visible)
        {
            slot = cb;
            if (b == null) return;
            b.gameObject.SetActive(visible);
            if (!visible) return;
            if (label != null) label.text = text;
            b.onClick.RemoveAllListeners();
            b.onClick.AddListener(() => slot?.Invoke());
        }

        private void Open(string title, string subtitle)
        {
            if (TitleText != null) TitleText.text = title;
            if (SubtitleText != null) SubtitleText.text = subtitle;
            gameObject.SetActive(true);
        }

        // Переход после награды: следующий день / выкуп(если можно) / вызов
        public void ShowTransition(bool hasNextDay, bool canRansom,
            Action onNextDay, Action onRansom, Action onChallenge)
        {
            Open("День завершён", "Что дальше?");
            Bind(PrimaryButton, PrimaryLabel, "Следующий день", onNextDay, ref primary, hasNextDay);
            Bind(SecondaryButton, SecondaryLabel, "Выкупить себя", onRansom, ref secondary, canRansom);
            Bind(TertiaryButton, TertiaryLabel, "Бросить вызов (КУСЬ)", onChallenge, ref tertiary, true);
        }

        public void ShowRansomWin()
        {
            Open("Свободный мимик",
                 "Поздравляю! Ты стал свободным мимиком. Ты нашёл... счастье?");
            Bind(PrimaryButton, PrimaryLabel, "В меню", ToMenu, ref primary, true);
            Bind(SecondaryButton, SecondaryLabel, "", null, ref secondary, false);
            Bind(TertiaryButton, TertiaryLabel, "", null, ref tertiary, false);
        }

        public void ShowChallengeStub()
        {
            Open("Вызов брошен", "[Боссфайт ещё не реализован — стаб]");
            Bind(PrimaryButton, PrimaryLabel, "В меню", ToMenu, ref primary, true);
            Bind(SecondaryButton, SecondaryLabel, "", null, ref secondary, false);
            Bind(TertiaryButton, TertiaryLabel, "", null, ref tertiary, false);
        }

        public void ShowDeath(Action onRetryDay)
        {
            Open("Здоровье на нуле",
                 "Большинство мимиков всю жизнь проводят незамеченными. О них не сложат легенд. А о тебе... может быть?");
            Bind(PrimaryButton, PrimaryLabel, "Переиграть день", onRetryDay, ref primary, true);
            Bind(SecondaryButton, SecondaryLabel, "В меню", ToMenu, ref secondary, true);
            Bind(TertiaryButton, TertiaryLabel, "", null, ref tertiary, false);
        }

        public void ShowBurst(Action onRetryDay)
        {
            Open("Ты лопнул от переедания",
                 "46% мимиков не доживают до старости из-за переедания. Вот кто ты есть: мимик обыкновенный.");
            Bind(PrimaryButton, PrimaryLabel, "Переиграть день", onRetryDay, ref primary, true);
            Bind(SecondaryButton, SecondaryLabel, "В меню", ToMenu, ref secondary, true);
            Bind(TertiaryButton, TertiaryLabel, "", null, ref tertiary, false);
        }

        private void ToMenu() => SceneManager.LoadScene("MainMenu");
    }
}
```

- [ ] **Step 2: Скомпилировать весь блок Task 7–10**

unityMCP `refresh_unity`, затем `read_console` (фильтр Error).
Ожидание: **0 ошибок компиляции**. Если есть — чинить (типичное: несовпадение имён методов/полей между `GameFlow`, попапами и `EndOfDayPopup`; сверить сигнатуры `Show*`).

- [ ] **Step 3: Прогнать все EditMode-тесты (регрессия)**

unityMCP `run_tests` mode=`EditMode` (без фильтра). Ожидание: все зелёные (старые + новые из Task 1–6).

- [ ] **Step 4: Закоммитить**

```bash
git add Game/Assets/Code/UI/EndOfDayPopup.cs
git commit -m "feat(ui): EndOfDayPopup — transition, ransom win, challenge stub, retry-day"
```

---

## Task 11: Конфиги — новые колонки, reward/punish/fixture предметы, дни

**Files:**
- Modify: `Game/Assets/Resources/Configs/loot.csv.txt`
- Modify: `Game/Assets/Resources/Configs/day.csv.txt`

- [ ] **Step 1: Обновить `loot.csv.txt`**

Заменить содержимое `Game/Assets/Resources/Configs/loot.csv.txt`. Заголовок + существующие предметы (новые колонки пустые/дефолтные) + новые. Колонки: `id,name,description,shape,gold,acidCost,healOnDigest,cellsRestoredOnDigest,adjacencyTarget,adjacencyEffect,category,acidRestoreOnDigest,damageOnDigest,canReturnToBasket,glue,neighborGoldPct`

```
id,name,description,shape,gold,acidCost,healOnDigest,cellsRestoredOnDigest,adjacencyTarget,adjacencyEffect,category,acidRestoreOnDigest,damageOnDigest,canReturnToBasket,glue,neighborGoldPct
sword,Меч,"Острый, но тупой",X|X|X|X,10,3,0,0,,,normal,0,0,1,0,0
shield,Щит,Защита от себя,XX|XX,8,4,5,0,sword,gold:+50%,normal,0,0,1,0,0
barracuda,Барракуда,Ещё трепыхается,X.|XX|X.|X.,15,5,0,2,bread,acid:-30%,normal,0,0,1,0,0
bread,Хлеб,Несвежий,XX,3,1,2,0,,,normal,0,0,1,0,0
gem,Самоцвет,Дешёвый блеск,X,20,2,0,0,gem,gold:+25%,normal,0,0,1,0,0
heart,Сердце,Бьётся,X,0,99,0,0,,,fixture,0,0,0,0,0
stomach,Желудок,Урчит,XX|XX,0,99,0,0,,,fixture,0,0,0,0,0
mimicburger,Мимик-бургер,Зубастый!,XX,0,3,8,0,,,reward,0,0,1,0,0
acidbottle,Бутылка кислоты,Шипит,X|X,0,2,0,0,,,reward,6,0,1,0,0
firecola,Огне-кола,Отрыжка пламенем,X,0,2,0,0,,,reward,0,0,1,0,0
weight,Гиря 60кг,Тяжёлая,XX|XX|XX,0,5,0,0,,,punish,0,30,0,0,0
poop,Какашка,Портит всё!,X,0,4,0,0,stomach,acid:+50%,punish,0,8,0,0,-50
glue,Масса клея,Липкая,XX,0,3,0,0,,,punish,0,4,0,1,0
tooth,Зуб с кариесом,Болит,X,0,9,0,0,,,punish,0,0,0,0,0
```

> `heart`/`stomach`: `acidCost=99` гарантирует, что их не переварят случайно (плюс `IsFixture` блокирует drag/digest — Task 12). `poop` рядом с `stomach` повышает свой расход ЖС (+50%) и снижает золото соседей (−50%).

- [ ] **Step 2: Обновить `day.csv.txt`**

Заменить содержимое `Game/Assets/Resources/Configs/day.csv.txt`. Колонки: `day,goldQuota,startHp,startAcid,adventurerIds,goldDamageMult,ransomGold`

```
day,goldQuota,startHp,startAcid,adventurerIds,goldDamageMult,ransomGold
1,40,5,15,warrior;rogue;mage,1,200
2,60,5,15,warrior;rogue;mage;warrior,1.5,200
3,90,5,15,warrior;rogue;mage;warrior;rogue,2,200
```

> `ransomGold=200` одинаков по дням (порог выкупа). `goldDamageMult` растёт к финалу. HP стартовый 5, переносится между днями.

- [ ] **Step 3: Проверить загрузку**

unityMCP `refresh_unity`, `read_console` (Error) — 0 ошибок. Затем Play-mode (Task 13) проверит фактический парс. Здесь просто убедиться, что `.txt` сохранены и не сломали импорт.

- [ ] **Step 4: Закоммитить**

```bash
git add Game/Assets/Resources/Configs/loot.csv.txt Game/Assets/Resources/Configs/day.csv.txt
git commit -m "feat(config): reward/punish/fixture loot + multi-day quotas"
```

---

## Task 12: Фикстуры (Сердце/Желудок) и блокировка drag/digest

**Files:**
- Modify: `Game/Assets/Code/Game/GameContext.cs` (спавн фикстур при старте, защита `Digest`)
- Modify: `Game/Assets/Code/Input/DragController.cs` (запрет таскать фикстуры)

- [ ] **Step 1: Прочитать DragController**

Открыть `Game/Assets/Code/Input/DragController.cs` целиком — найти, где начинается захват предмета (вероятно по `LootView` под курсором). Цель: в начале drag-захвата добавить ранний выход `if (view.Data.IsFixture) return;`.

- [ ] **Step 2: Запретить таскать фикстуры**

В месте начала перетаскивания в `DragController` добавить проверку (точную строку определить по чтению Step 1; пример):

```csharp
            if (picked != null && picked.Data != null && picked.Data.IsFixture)
                return; // сердце/желудок не двигаются
```

- [ ] **Step 3: Защитить Digest от фикстур**

В `Game/Assets/Code/Game/GameContext.cs` метод `Digest`, первой строкой:

```csharp
        public void Digest(LootView item)
        {
            if (item.Data.IsFixture) return;
```

И учесть восстановление ЖС/урон при переваривании (награды/наказания) — расширить тело `Digest`:

```csharp
            int cost = LastResolved != null ? LastResolved.GetAcid(item) : item.Data.AcidCost;
            if (Resources.CurrentAcid < cost) return;
            Resources.CurrentAcid -= cost;
            Resources.CurrentAcid += item.Data.AcidRestoreOnDigest; // кислота восполняет ЖС
            Resources.CurrentHp += item.Data.HealOnDigest;          // бургер лечит
            Resources.CurrentHp -= item.Data.DamageOnDigest;        // гиря/клей/какашка бьют
            MimicGrid.Model.Remove(item);
            Destroy(item.gameObject);
            OnGridChanged();
            if (Resources.CurrentHp <= 0 && GameFlowDeathHook != null) GameFlowDeathHook();
        }

        public System.Action GameFlowDeathHook; // GameFlow подписывается, чтобы ловить смерть от переваривания
```

> Подписку `GameFlowDeathHook` добавить в `GameFlow.Start`: `GameContext.Instance.GameFlowDeathHook = () => { if (Phase == DayPhase.Adventurers) EndDeath(); };` — смерть от переваривания гири во время дня = поражение. Для этого сделать `EndDeath()` доступным (он уже private в том же классе — обернуть в метод или вызвать через lambda в Start, где он в scope). Добавить в `GameFlow.Start` после установки слушателей.

- [ ] **Step 4: Спавнить фикстуры при первом дне**

В `GameContext` добавить метод и вызвать его из `GameFlow.BeginDay(firstDay:true)` **один раз** (фикстуры переживают дни вместе с лутом):

```csharp
        public void SpawnFixtures()
        {
            PlaceFixture("heart", 0, MimicGrid.Height - 2);
            PlaceFixture("stomach", MimicGrid.Width - 3, 1);
        }

        private void PlaceFixture(string id, int x, int y)
        {
            var data = LootCatalog.Get(id);
            var view = SpawnLoot(data, MimicGrid.CellsRoot);
            if (MimicGrid.Model.TryPlace(view, x, y, Mimic.Logic.Rotation.Deg0))
            {
                var rt = (RectTransform)view.transform;
                rt.SetParent(MimicGrid.CellsRoot, false);
                rt.anchorMin = rt.anchorMax = rt.pivot = new Vector2(0, 0);
                rt.position = MimicGrid.CellRects[x, y].position;
            }
            else Destroy(view.gameObject);
        }
```

В `GameFlow.BeginDay`, внутри `if (firstDay)` ветки (т.е. оборачиваем существующий первый вызов), добавить `GameContext.Instance.SpawnFixtures();` для `firstDay`.

- [ ] **Step 5: Скомпилировать**

unityMCP `refresh_unity`, `read_console` (Error). Ожидание: 0 ошибок.

- [ ] **Step 6: Закоммитить**

```bash
git add Game/Assets/Code/Game/GameContext.cs Game/Assets/Code/Input/DragController.cs Game/Assets/Code/Game/GameFlow.cs
git commit -m "feat(fixtures): heart/stomach tokens, digest effects, death-on-digest hook"
```

---

## Task 13: Сборка сцены + Play-mode smoke-тест полного лупа

**Files:**
- Modify: `Game/Assets/Scenes/Game.unity` (через unityMCP: добавить попапы и проставить ссылки)

В сцене нужны новые объекты: `OverlordPopup`, `RewardChoicePopup` и кнопки `Primary/Secondary/Tertiary` в `EndOfDayPopup`. Используем unityMCP для создания UI и привязки ссылок на `GameFlow`.

- [ ] **Step 1: Открыть сцену и осмотреть текущий HUD/попапы**

unityMCP `manage_scene` load `Assets/Scenes/Game.unity`; `find_gameobjects` — найти `GameFlow`, `HudView`, существующий `EndOfDayPopup`, Canvas. Зафиксировать иерархию.

- [ ] **Step 2: Создать OverlordPopup UI**

Через `manage_gameobject`/`manage_ui`: под Canvas создать панель `OverlordPopup` (скрыта) c: `TitleText`, `GoldText`, `SettleButton`+`SettleLabel`, `TooltipText`. Навесить компонент `Mimic.UI.OverlordPopup` (`manage_components`), проставить ссылки полей на созданные объекты (`Root` = сама панель).

- [ ] **Step 3: Создать RewardChoicePopup UI**

Аналогично: панель `RewardChoicePopup` (скрыта) c `TitleText` и 3 кнопками `OptionButtons[0..2]`+`OptionLabels`. Навесить `Mimic.UI.RewardChoicePopup`, проставить массивы ссылок.

- [ ] **Step 4: Расширить EndOfDayPopup UI**

Добавить в существующий `EndOfDayPopup` три кнопки `PrimaryButton/SecondaryButton/TertiaryButton` с дочерними `Text` лейблами; проставить ссылки в компонент. (Старые `RetryButton/MenuButton` больше не используются кодом — можно оставить скрытыми или удалить.)

- [ ] **Step 5: Связать ссылки в GameFlow**

В компоненте `GameFlow` проставить `OverlordPopup` и `RewardPopup` (drag refs через `manage_components` set property).

- [ ] **Step 6: Play-mode smoke — день 1 happy path**

unityMCP `manage_editor` enter Play. Сценарий:
1. Принять всех приключенцев (через intro-попап «КУСЬ»), разложить лут в мимик.
2. Нажать «Завершить день» → появляется `OverlordPopup`, `GoldText` = `[Total]/40`.
3. (Опц.) перетащить часть лута в правый грид (корзина).
4. Нажать «Подвести итог дня».
   - `read_console`: нет исключений.
   - Если Total≥40 → `RewardChoicePopup` с наградами; иначе с наказаниями + HP упал на `(40-Total)*1`.
5. Забрать 1 предмет → он лёг в мимик → появился `EndOfDayPopup` «День завершён» с кнопкой «Следующий день».
6. «Следующий день» → День 2, HUD `День 2`, ЖС полный, HP сохранился, лут на месте.

Ожидание: проходит без ошибок в консоли; HP/ЖС/день/золото меняются корректно.

- [ ] **Step 7: Play-mode — концовки**

- Накопить TotalGold ≥ 200 (отредактировать `ransomGold` на 0 временно ИЛИ задержать) → в Transition видна кнопка «Выкупить себя» → клик → экран «Свободный мимик».
- «Бросить вызов» → стаб-экран.
- Провалить квоту до HP≤0 → «Здоровье на нуле» → «Переиграть день» перезапускает текущий день.

- [ ] **Step 8: Сохранить сцену и закоммитить**

unityMCP `manage_scene` save. Затем:

```bash
git add Game/Assets/Scenes/Game.unity
git commit -m "feat(scene): wire Overlord/Reward popups + end-of-day buttons"
```

---

## Task 14: Склейка клеем (масса клея) — перенос соседей вместе

**Files:**
- Modify: `Game/Assets/Code/Input/DragController.cs`
- Test: `Game/Assets/Code.Tests/GlueGroupTests.cs` (создать — для чистой группировки)

ТЗ: предметы, соприкасающиеся гранями с массой клея, переносятся **только вместе с ней**. Минимальная jam-реализация: при попытке перетащить предмет, склеенный с клеем (4-сосед клея), тащим всю склеенную группу как единый набор смещений. Группировка — чистая логика (тестируема); применение к drag — в DragController.

- [ ] **Step 1: Написать падающий тест группировки**

Создать `Game/Assets/Code.Tests/GlueGroupTests.cs`:

```csharp
using NUnit.Framework;
using Mimic.Logic;
using Mimic.Data;
using System.Collections.Generic;

namespace Mimic.Tests
{
    public class GlueGroupTests
    {
        private class Item { public string Id; public Shape Shape; public bool IsGlue; }
        private static Item Mk(string id, bool glue = false) =>
            new Item { Id = id, Shape = Shape.Parse("X"), IsGlue = glue };

        [Test]
        public void Group_GlueWithTwoNeighbors_ReturnsAllThree()
        {
            var grid = new GridModel<Item>(4, 4);
            var glue = Mk("glue", true);
            var a = Mk("a");
            var b = Mk("b");
            grid.TryPlace(glue, 1, 1, Rotation.Deg0);
            grid.TryPlace(a, 0, 1, Rotation.Deg0); // слева от клея
            grid.TryPlace(b, 2, 1, Rotation.Deg0); // справа
            var group = GlueGroup.Resolve(grid, a, i => i.IsGlue);
            Assert.Contains(glue, group);
            Assert.Contains(a, group);
            Assert.Contains(b, group);
            Assert.AreEqual(3, group.Count);
        }

        [Test]
        public void Group_ItemNotTouchingGlue_ReturnsItselfOnly()
        {
            var grid = new GridModel<Item>(4, 4);
            var a = Mk("a");
            var glue = Mk("glue", true);
            grid.TryPlace(a, 0, 0, Rotation.Deg0);
            grid.TryPlace(glue, 3, 3, Rotation.Deg0);
            var group = GlueGroup.Resolve(grid, a, i => i.IsGlue);
            Assert.AreEqual(1, group.Count);
            Assert.Contains(a, group);
        }
    }
}
```

- [ ] **Step 2: Прогнать — FAIL (нет `GlueGroup`)**

unityMCP `run_tests` EditMode, filter `Mimic.Tests.GlueGroupTests`.

- [ ] **Step 3: Реализация `GlueGroup`**

Создать `Game/Assets/Code/Logic/GlueGroup.cs`:

```csharp
using System;
using System.Collections.Generic;

namespace Mimic.Logic
{
    public static class GlueGroup
    {
        // Если item НЕ касается клея — вернёт {item}.
        // Если касается — вернёт связную группу: клей + все его прямые 4-соседи.
        public static List<T> Resolve<T>(GridModel<T> grid, T item, Func<T, bool> isGlue) where T : class
        {
            var single = new List<T> { item };
            // найти клей среди 4-соседей item
            T glue = isGlue(item) ? item : FindGlueNeighbor(grid, item, isGlue);
            if (glue == null) return single;

            var group = new List<T> { glue };
            foreach (var nb in Neighbors4(grid, glue))
                if (!group.Contains(nb)) group.Add(nb);
            if (!group.Contains(item)) group.Add(item);
            return group;
        }

        private static T FindGlueNeighbor<T>(GridModel<T> grid, T item, Func<T, bool> isGlue) where T : class
        {
            foreach (var nb in Neighbors4(grid, item))
                if (isGlue(nb)) return nb;
            return null;
        }

        private static IEnumerable<T> Neighbors4<T>(GridModel<T> grid, T item) where T : class
        {
            var seen = new HashSet<T>();
            int[] dx = { 1, -1, 0, 0 };
            int[] dy = { 0, 0, 1, -1 };
            for (int x = 0; x < grid.Width; x++)
                for (int y = 0; y < grid.Height; y++)
                {
                    if (!ReferenceEquals(grid.GetAt(x, y), item)) continue;
                    for (int k = 0; k < 4; k++)
                    {
                        var nb = grid.GetAt(x + dx[k], y + dy[k]);
                        if (nb != null && !ReferenceEquals(nb, item) && seen.Add(nb)) yield return nb;
                    }
                }
        }
    }
}
```

- [ ] **Step 4: Прогнать — PASS**

unityMCP `run_tests` EditMode, filter `Mimic.Tests.GlueGroupTests`. Ожидание: 2 PASS.

- [ ] **Step 5: Подключить к DragController (поведенческая часть)**

В `DragController` при старте перетаскивания вычислить группу через `GlueGroup.Resolve(MimicGrid.Model, picked, v => v.Data.IsGlue)`. Если в группе >1 — двигать все элементы синхронно (запомнить относительные смещения клеток и при отпускании размещать группу единым блоком; если хоть один не влезает — откатить весь перенос). Точную интеграцию реализовать по месту (зависит от модели drag в `DragController`, прочитанной в Task 12 Step 1). Если интеграция в drag слишком объёмна для оставшегося времени джема — оставить `GlueGroup` логику готовой и подключить как доработку; пометить в коде `// TODO(jam): group drag wiring`.

> ⚠️ Это единственное место с допустимым «по месту»: drag-движок не виден из спека. `GlueGroup` логика полностью готова и протестирована — остаётся только её применение.

- [ ] **Step 6: Закоммитить**

```bash
git add Game/Assets/Code/Logic/GlueGroup.cs Game/Assets/Code.Tests/GlueGroupTests.cs Game/Assets/Code/Input/DragController.cs
git commit -m "feat(glue): glued-group resolution + drag wiring"
```

---

## Task 15: Финальная регрессия и проверка покрытия спека

- [ ] **Step 1: Все EditMode-тесты зелёные**

unityMCP `run_tests` mode=`EditMode` (без фильтра). Ожидание: 100% PASS (старые + GameResources, LootData, DayConfig, Theft, Settlement, NeighborGold, GlueGroup).

- [ ] **Step 2: Полный Play-mode прогон 3 дней**

Пройти День 1→2→3 happy-path, затем на День 3 (последний) убедиться: кнопки «Следующий день» нет, «Выкупить себя» доступна при Total≥200, «Бросить вызов» всегда. `read_console` — ноль исключений за весь прогон.

- [ ] **Step 3: Свериться со спеком**

Открыть `docs/superpowers/specs/2026-05-29-blue-loop-design.md` и пройти §1–§6: каждое требование → есть в реализации. Зафиксировать осознанные деферралы (боевые эффекты инертны при стабе боссфайта; откат лута при «Переиграть день» не воспроизводит точное расположение — только HP/Banked).

- [ ] **Step 4: Финальный коммит (если были правки)**

```bash
git add -A
git commit -m "chore(blue-loop): final regression pass"
```

---

## Осознанные деферралы (jam)

- **Боевые эффекты** предметов (урон врагу у кислоты/огне-колы, блок КУСЬ и −50% исходящего урона у зуба) — инертны, т.к. боссфайт = стаб. Данные в конфиге заложены, подключатся в красном/жёлтом лупе.
- **«Переиграть день»** откатывает HP и BankedGold к снапшоту, но не восстанавливает точное расположение лута в сетке (jam-упрощение).
- **Корзина** при банковании берёт **базовое** золото предмета (без adjacency), чтобы не усложнять; adjacency считается только для лута, остающегося в мимике.
- **Склейка клеем**: чистая группировка готова и протестирована; синхронный drag-перенос группы — облегчённая интеграция, помечен `TODO(jam)` если не успеть.
- **«Нельзя вернуть в корзину»** (`CanReturnToBasket=false` у наказаний): запрет дропа таких предметов в корзинный грид во время фазы Властелина в MVP не форсируется на уровне drag (требует правки DragController под фазу). Поведенчески наказания и так невыгодно сдавать (их золото = 0). Подключить запрет дропа как доработку, если останется время.
